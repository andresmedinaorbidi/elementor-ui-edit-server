# Google AI (Gemini) API key – get from https://aistudio.google.com
GOOGLE_API_KEY=
# Or use GEMINI_API_KEY (same key)

# Model name (default: gemini-2.5-flash)
GEMINI_MODEL=gemini-2.5-flash

# Server port (default: 3000)
PORT=3000

# Optional: require X-Service-Key header matching this value (leave empty to disable)
SERVICE_SECRET=

# Service supports full AI Elementor Sync contract (text, url, image) when plugin sends image_slots and edit_capabilities

# Optional: LangSmith tracing
# LANGSMITH_API_KEY=
# LANGSMITH_TRACING=true
